spring:
  application:
    name: producer
  profiles:
    active: hazelcast

producer:
  itemsToProduce: 10
  parallelism: 2
  minPayloadSize: 1024
  maxPayloadSize: 1024

logging:
  level:
    com.devkwondo: DEBUG

server:
  port: 0